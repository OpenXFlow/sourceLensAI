# This inventory is typically generated dynamically from Terraform output.
# For this example, we'll use placeholders.
[webservers]
# web_server_ip ansible_user=ubuntu

[appservers]
# app_server_ip ansible_user=ubuntu